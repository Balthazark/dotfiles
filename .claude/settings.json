{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "grep -q '\"file_path\":\"/.*\\.nix\"' && (cd ~/git/dotfiles && nix fmt && statix check ~/git/dotfiles && deadnix ~/git/dotfiles) 2>/dev/null || true",
            "statusMessage": "Formatting and linting nix files..."
          }
        ]
      }
    ]
  }
}
