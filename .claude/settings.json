{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -q '\\.nix$' && (cd ~/git/dotfiles && nix fmt && statix check ~/git/dotfiles && deadnix ~/git/dotfiles) 2>/dev/null || true",
            "statusMessage": "Formatting and linting nix files..."
          }
        ]
      }
    ]
  }
}
